# Lightweight test/dev dependency set.
#
# The full requirements.txt pulls torch + whisperx + pyannote (~14 GB image).
# The unit suite does not need any of that: tests/conftest.py stubs the
# inference pipeline, so every route, the ASR router, the stitcher, the OIDC
# bridge, and the role-labelling logic are exercised against pure-Python code.
#
# Use this for local test runs and CI:
#     python -m venv .venv && . .venv/bin/activate
#     pip install -r requirements-dev.txt
#     pytest -q
#
# Requires Python 3.11+ (the code uses PEP 604 `X | None` annotations, which
# FastAPI/Pydantic must evaluate at runtime).

# --- Runtime deps the tests import directly ---
fastapi==0.115.5
python-multipart==0.0.18
pydantic==2.10.3
pydantic-settings==2.7.0
httpx==0.27.2
PyJWT[crypto]==2.10.1
python-dateutil==2.9.0
# Pinned <2 to match requirements.txt (pyannote 3.1.1 / torch 2.0.1 ABI).
numpy>=1.24,<2

# --- Test tooling ---
pytest>=8.0,<9.0
pytest-asyncio==0.24.0
