# GPU diarization sidecar deps. Independent of the main service's frozen pins —
# this stack is modern precisely so it can run on Blackwell (RTX 5090, sm_120),
# which needs CUDA 12.8 + torch >= 2.7. cu128 wheels bundle the CUDA runtime.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.8.0
torchaudio==2.8.0

# pyannote.audio 3.3.x works with modern torch/torchaudio (it dropped the
# torchaudio.set_audio_backend call that forced the main service to pin torch
# 2.0.x) and still loads the pyannote/speaker-diarization-3.1 pipeline, so
# diarization accuracy is comparable to the CPU path for the before/after.
pyannote.audio==3.3.2

fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.18
